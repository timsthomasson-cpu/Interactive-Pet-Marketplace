import { PageShell } from "@/components/layout";
import { products } from "@/components/site-data";
import { SectionHeading } from "@/components/ui";
import Link from "next/link";
export default function ComparePage() {
  return <PageShell><section className="section-pad"><div className="container-shell"><SectionHeading eyebrow="Comparison table" title="Compare comfort, complexity, and price in one place." text="This page is meant to reduce friction for buyers who already know they want a smart pet but need a clean side-by-side view." /><div className="mt-10 overflow-hidden rounded-3xl border border-coral-200 bg-white shadow-soft"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-cream-100"><tr>{["Product","Type","Best For","Key Features","Highlight","Price","Action"].map((h)=><th key={h} className="px-5 py-4 font-semibold text-slate-900">{h}</th>)}</tr></thead><tbody>{products.map((product)=><tr key={product.slug} className="border-t border-coral-200 align-top"><td className="px-5 py-4 font-semibold text-slate-900">{product.name}</td><td className="px-5 py-4 text-slate-600">{product.type}</td><td className="px-5 py-4 text-slate-600">{product.bestFor.join(", ")}</td><td className="px-5 py-4 text-slate-600">{product.features.join(", ")}</td><td className="px-5 py-4 text-slate-600">{product.highlight}</td><td className="px-5 py-4 text-slate-900">{product.price}</td><td className="px-5 py-4"><Link href={product.productUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center justify-center rounded-full bg-trust-500 px-5 py-3 text-sm font-semibold text-white transition hover:-translate-y-0.5 hover:bg-trust-600 whitespace-nowrap">View Details</Link></td></tr>)}</tbody></table></div></div></div></section></PageShell>;
}
