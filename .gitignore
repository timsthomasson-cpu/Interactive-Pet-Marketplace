node_modules/
.next/
.env
.vercel

# Cloudflare / OpenNext build output and generated files
.open-next/
.wrangler/
cloudflare-env.d.ts
.dev.vars

# Office lock files (created when Excel/Word has a file open)
~$*

# OS-generated files
.DS_Store
Thumbs.db

# Local-only debug capture of Meta Pixel events (dev debugging only,
# never written to in production — see app/api/debug/view-content).
Data/*.jsonl
